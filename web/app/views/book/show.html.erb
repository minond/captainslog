<%= render "book/nav" %>

<br>
<br>

<%= form_for(@book, :url => book_path(@book)) do |f| %>
  <textarea name="text" placeholder="<%= t(:enter_new_log) %>" class="entry-textarea"></textarea>
<% end %>
