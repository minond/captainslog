<textarea
  name="<%= config[:name] || 'text' %>"
  rows="<%= config[:rows] || '1' %>"
  placeholder="<%= config[:requested_time] ? t(:enter_new_log, :datetime => config[:requested_time].to_date) : '' %>"
  class="entry-input"
  autofocus="true"
  data-enter-to-submit="true"
><%= config[:text] || "" %></textarea>
