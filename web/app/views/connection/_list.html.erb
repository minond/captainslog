<div class="table">
  <table class="table">
    <tr>
      <th><%= t(:data_source) %></td>
      <th><%= t(:book) %></td>
    </tr>
    <% connections.each do |connection| %>
      <tr>
        <td><%= connection.data_source %></td>
        <td><%= connection&.book&.slug %></td>
      </tr>
    <% end %>
  </table>
</div>
