<header>
  <table>
    <tr>
      <td>
        <%= link_to image_tag("logo.png"), root_path, :class => "logo" %>
      </td>
      <td>
        <% if current_user %>
          <form action="<%= search_path %>" class="search">
            <label>
              <%= image_tag("search.svg") %>
              <input type="text" name="query" />
            </label>
          </form>
          <%= link_to current_user.display_character, user_path(current_user), :class => "circular" %>
        <% end %>
      </td>
    </tr>
  </table>
</header>
