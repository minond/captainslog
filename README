Captain's log is a personal logging application.

Make sure to run `go generate` with Go modules disabled -- Kallax's generator
does not like to be ran with modules:

    GO111MODULE=off go generate ./...
